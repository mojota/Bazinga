<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/color_theme_dark</item>
        <item name="colorPrimary">@color/color_theme</item>
        <item name="android:windowContentTransitions">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="Toolbar_Title">
        <item name="android:textSize">18dp</item>
        <item name="android:textColor">#000000</item>
    </style>

    <style name="anim_menuPop">
        <item name="android:windowEnterAnimation">@anim/anim_menu_in</item>
        <item name="android:windowExitAnimation">@anim/anim_menu_out</item>
    </style>
</resources>
